environment: development

logging:
  level:
    root: DEBUG

spring:
  datasource:
    url: jdbc:h2:mem:marketplace
  jpa:
    generate-ddl: true
    show-sql: true
    defer-datasource-initialization: true
